# Screenshots

After testing the workflow, take your own screenshots:

1. **Workflow Canvas** — In n8n, with the full workflow visible
2. **Sample Output** — The Discord/Slack message with the generated summary
3. **Execution Log** — A successful run in the n8n executions tab

Save them as `workflow.png`, `output.png`, and `execution.png` in this directory.
